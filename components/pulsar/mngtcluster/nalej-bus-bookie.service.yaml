apiVersion: v1
kind: Service
metadata:
  name: bookie
  namespace: nalej
  labels:
    app: bookie
    component: nalej-bus
    cluster: management
spec:
  ports:
    - port: 3181
      name: server
  type: ClusterIP
  selector:
    app: bookie
    component: nalej-bus
    cluster: management