apiVersion: batch/v1
kind: Job
metadata:
  name: pulsar-cluster-metadata-init
  namespace: nalej
  labels:
    app: pulsar
    component: broker
spec:
  template:
    spec:
      containers:
        - name: pulsar-cluster-metadata-init-container
          image: apachepulsar/pulsar:2.3.1
          command: ["sh", "-c"]
          args:
            - >
              bin/pulsar initialize-cluster-metadata \
                --cluster local \
                --zookeeper zookeeper \
                --configuration-store zookeeper \
                --web-service-url http://broker.nalej.svc.cluster.local:8080/ \
                --broker-service-url pulsar://broker.nalej.svc.cluster.local:6650/ || true;
      restartPolicy: Never
